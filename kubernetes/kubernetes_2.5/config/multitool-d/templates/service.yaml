apiVersion: v1
kind: Service
metadata:
  name: {{ include "multitool-d.fullname" . }}
  labels:
    {{- include "multitool-d.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multitool-d.selectorLabels" . | nindent 4 }}
