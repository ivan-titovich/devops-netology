apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "multi-test.fullname" . }}-test-connection"
  labels:
    {{- include "multi-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "multi-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
